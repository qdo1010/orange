namespace FetchGame;

enum ServerControl : byte {
    IDLE,
    OPENCAMERA,
    STARTTHREAD,
    STARTRECORDING,
    STOPRECORDING,
    QUIT,
    TESTFOCUS,
    SETFOCUS,
    STARTSTREAM,
    LJEDGE
}

enum SignalType: byte {
    ClientBringup,
    ClientStateUpdate,
    INDIGO,
    INDIGO_TRIAL_TRIGGER,
    CalibrationNextPose,
    CalibrationPoseReached,
    CalibrationDone
}

enum ManagerState: byte {
    IDLE,
    CONNECT,
    CONNECTED,
    OPENCAMERA,
    CAMERAOPENED,
    ERROR,
    STARTCAMTHREAD,
    THREADREADY,
    RECORDSTOPPED,
    WAITTHREAD,
    WAITSTART,
    WAITSTOP
}

table bring_up_message {
    server_name:string;
    num_cameras:short;
}

table Server {
    signal_type:SignalType = ClientBringup;
    control:ServerControl = IDLE;
    server_mesg:bring_up_message;
    config_folder:string;
    record_folder:string;
    encoder_setup:string;
    ptp_global_time:ulong;
    server_state:ManagerState = IDLE;
    focus_value:int;
    camera_serial:string;
    lj_trigger_mode:bool = false;
    lj_edge_index:ulong = 0;
    lj_edge_timestamp_ns:ulong = 0;
    lj_frames_per_edge:int = 1;
}

root_type Server;